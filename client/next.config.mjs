/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "tailwindui.com",
      "dajocode-cloud-storage-api.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
